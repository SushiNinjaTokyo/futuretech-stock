<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ date }} — Top 10 | FutureTech Markets</title>
  <meta name="description" content="Top 10 movers for {{ date }} across AI, Space, and Robotics">
  <link rel="canonical" href="https://your-domain.com/daily/{{ date }}.html">
  <script type="application/ld+json">{{ schema_json | safe }}</script>
  <link rel="preload" as="style" href="/assets/styles.css">
  <link rel="stylesheet" href="/assets/styles.css">
  <meta property="og:title" content="Top 10 — {{ date }} | FutureTech Markets">
  <meta property="og:description" content="AI × Space × Robotics — price, news count, and abnormal volume.">
  <meta property="og:type" content="article">
  <meta property="og:url" content="https://your-domain.com/daily/{{ date }}.html">
  <meta name="twitter:card" content="summary_large_image">
</head>
<body>
  <header>
    <h1>Top 10 — {{ date }}</h1>
    <p>Scored by price change, news count, and abnormal volume.</p>
    <nav><a href="/">Home</a> · <a href="/daily/index.html">All Reports</a></nav>
  </header>
  <main>
    <ol>
    {% for row in top10 %}
      <li>
        <h2>{{ row.symbol }} — {{ row.name }} <small>({{ row.theme }})</small></h2>
        <p>Price Δ: {{ "%.2f"|format(row.pct_change*100) }}% · News: {{ row.news_count }} · Volume x{{ "%.1f"|format(row.vol_ratio) }}</p>
        <div><img src="/charts/{{ date }}/{{ row.symbol }}.png" alt="Chart {{ row.symbol }}" width="640" onerror="this.style.display='none'"></div>
        <details><summary>Technical snapshot</summary><pre>{{ row.tech_note }}</pre></details>
        <details><summary>IR/News summary</summary><pre>{{ row.ir_note }}</pre></details>
      </li>
    {% endfor %}
    </ol>
  </main>
  <footer><small>© FutureTech Markets</small></footer>
</body>
</html>
