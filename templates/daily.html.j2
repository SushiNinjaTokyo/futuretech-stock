<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>FutureTech Stock Daily</title>
  <style>
    body {
      font-family: 'Inter', sans-serif;
      margin: 0;
      padding: 0;
      background: #f8fafc;
      color: #0f172a;
    }
    header {
      padding: 1rem;
      background: #0f172a;
      color: #fff;
      text-align: center;
    }
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 1rem;
    }
    .toggle-btn {
      display: inline-block;
      margin: 1rem 0;
      padding: 0.6rem 1.2rem;
      border: none;
      border-radius: 20px;
      background: #334155;
      color: #fff;
      font-size: 0.9rem;
      font-weight: 500;
      cursor: pointer;
      transition: background 0.3s ease;
    }
    .toggle-btn:hover {
      background: #475569;
    }
    .overview {
      display: none;
      margin-top: 1rem;
      padding: 1rem;
      background: #fff;
      border-radius: 12px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.08);
    }
    .legend {
      margin-top: 2rem;
      font-size: 0.85rem;
      line-height: 1.5;
      color: #475569;
      background: #f1f5f9;
      padding: 1rem;
      border-radius: 10px;
    }
    .legend h4 {
      margin-top: 0;
      color: #0f172a;
      font-size: 0.95rem;
      font-weight: 600;
    }
    /* Rank styling (gold/silver/bronze for top3) */
    .rank-rocket.rank1 { background: radial-gradient(circle, #FFD700, #b58900); }
    .rank-rocket.rank2 { background: radial-gradient(circle, #C0C0C0, #6e6e6e); }
    .rank-rocket.rank3 { background: radial-gradient(circle, #cd7f32, #6e3c1f); }
    .rank-rocket {
      width: 60px;
      height: 60px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-right: 0.5rem;
      position: relative;
    }
    .rank-rocket svg {
      width: 28px;
      height: 28px;
    }
    .rank-num {
      position: absolute;
      bottom: -8px;
      right: -8px;
      font-size: 0.8rem;
      font-weight: 700;
      color: #fff;
      background: rgba(0,0,0,0.4);
      padding: 2px 6px;
      border-radius: 50%;
    }
    @media (max-width: 768px) {
      .rank-rocket { width: 50px; height: 50px; }
      .rank-rocket svg { width: 24px; height: 24px; }
    }
  </style>
  <script>
    function toggleOverview() {
      const section = document.getElementById("overview");
      section.style.display = section.style.display === "block" ? "none" : "block";
    }
  </script>
</head>
<body>
  <header>
    <h1>FutureTech Stock Daily Report</h1>
  </header>

  <div class="container">
    <button class="toggle-btn" onclick="toggleOverview()">Toggle Overview</button>

    <div id="overview" class="overview">
      <h2>Overview</h2>
      <p>This section provides detailed snapshots of selected stocks across multiple indicators.</p>

      <!-- Legend -->
      <div class="legend">
        <h4>Legend</h4>
        <ul>
          <li><b>Price changes</b>: 1D = vs last trading day, 1W = vs 5 trading days ago, 1M = vs 20 trading days ago.</li>
          <li><b>Trends</b>: Google Trends score, normalized within selected stocks.</li>
          <li><b>News</b>: Coverage × sentiment, normalized within selected stocks.</li>
          <li><b>Volume anomaly</b>: Abnormal trading volume relative to recent average.</li>
          <li><b>DII</b>: Demand Imbalance Indicator, normalized within selected stocks.</li>
          <li><b>Total Score</b>: Weighted integration of all indicators.</li>
        </ul>
      </div>
    </div>

    <!-- Ranking rows -->
    {% for sym, pts, rank in ranking %}
    <div class="row">
      <div class="rank-rocket rank{{ rank if rank <= 3 else 'N' }}">
        <svg viewBox="0 0 24 24" fill="white">
          <path d="M12 2C8 5 8 12 12 22c4-10 4-17 0-20z"/>
        </svg>
        {% if rank > 3 %}
          <div class="rank-num">{{ rank }}</div>
        {% endif %}
      </div>
      <div class="info">
        <h3>{{ sym }}</h3>
        <p>Score: {{ pts|round(2) }}</p>
      </div>
    </div>
    {% endfor %}
  </div>
</body>
</html>
